<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leyi.snack.mapper.OrderItemMapper">

    <select id="selectById" resultType="com.leyi.snack.entity.OrderItem">
        SELECT * FROM order_item WHERE id = #{id}
    </select>

    <insert id="batchSave">
        INSERT INTO order_item (order_id, goods_id, goods_name, goods_image, price, quantity)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.goodsId}, #{item.goodsName}, #{item.goodsImage}, #{item.price}, #{item.quantity})
        </foreach>
    </insert>

    <select id="selectByOrderId" resultType="com.leyi.snack.entity.OrderItem">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>

    <select id="selectTopSelling" resultType="com.leyi.snack.vo.DashboardFullVO$TopGoods">
        SELECT 
            goods_name as goodsName, 
            SUM(quantity) as quantity
        FROM order_item
        GROUP BY goods_id, goods_name
        ORDER BY quantity DESC
        LIMIT #{limit}
    </select>

    <!-- 品类销售占比 -->
    <select id="selectCategorySalesShare" resultType="com.leyi.snack.vo.DashboardFullVO$CategoryShare">
        SELECT 
            c1.name as name,
            SUM(oi.price * oi.quantity) as value
        FROM order_item oi
        JOIN goods g ON oi.goods_id = g.id
        JOIN category2 c2 ON g.category2_id = c2.id
        JOIN category1 c1 ON c2.parent_id = c1.id
        JOIN `order` o ON oi.order_id = o.id
        WHERE o.status = 1
        GROUP BY c1.name
    </select>
</mapper>
