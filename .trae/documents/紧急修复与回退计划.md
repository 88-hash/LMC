# 🚑 紧急修复计划 (Emergency Fix Plan)

**问题确认**: 
1. `Home.vue` 存在 JavaScript 语法错误 (`catch` 语句缺失花括号)，导致 Vite 编译失败 (500 Error)，主页无法加载。
2. `index.html` 中的 `user-scalable=no` 限制了用户缩放，影响了网页体验。

## 🛠️ 修复步骤

### 1. 修复 `src/views/Home.vue`
- **动作**: 修正 3 处语法错误。
- **修改前**: `catch (e) console.error(e)`
- **修改后**: `catch (e) { console.error(e) }`

### 2. 回退 `index.html` 视口设置
- **动作**: 移除禁止缩放的属性，恢复标准 Web 体验。
- **内容**: 删除 `maximum-scale=1.0, user-scalable=no`。

### 3. 重启服务与验证 (新增标准流程)
- **重启后端**: 停止并重新运行 Spring Boot 后端服务，确保数据接口正常。
- **刷新前端**: 确保前端开发服务器热更新或重启，加载最新代码。

---
*执行完毕后，我将确保后端在运行，前端页面无报错。*