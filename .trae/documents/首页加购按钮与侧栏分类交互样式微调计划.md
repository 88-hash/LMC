## 目标
- 修正商品卡片的“加号”按钮位置到卡片右下角；避免与价格文字冲突
- 侧栏分类项保持单行不换行，扩大点击/可视区域，提升触控体验
- 仅改前端样式，不动逻辑与后端

## 修改文件
- frontend/src/views/Home.vue（仅 <style scoped> 样式区）

## 具体改动
1) 加号按钮定位与避让
- .goods-card：添加 position: relative
- .add-btn：改为 position: absolute; right: 12px; bottom: 12px; 尺寸 40×40；保持圆形渐变与阴影
- .goods-bottom：增加右侧内边距以避让按钮：padding-right: 56px
- 价格文字仍左侧对齐，保证按钮悬浮在右下角且不覆盖文字

2) 侧栏分类单行与区域扩大
- .sidebar-item：提高触控尺寸（高度 44–48px），使用 display:flex; align-items:center; 调整左右内边距
- .item-text：white-space: nowrap; overflow: hidden; text-overflow: ellipsis; 宽度 100%，保持单行省略
- 选中态（.sidebar-item.active）保留“白色胶囊+主色文字”，但不改变文本换行行为

## 验证
- 首页商品卡：加号固定右下角，价格不被遮挡
- 侧栏分类：中文标题不换行，长标题显示为单行省略；点击区域更大，滚动稳定

## 风险与回滚
- 全为样式微调，若效果不符可恢复原有 CSS；不涉及逻辑与模板更改

确认后，我将按本方案实施并提交变更以供你预览。