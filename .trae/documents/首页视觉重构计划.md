# 🎨 首页视觉重构计划 (Home Visual Overhaul Plan)

**状态确认**: 用户明确要求进行 Plan Mode，并且提供了非常详细的 CSS 和 HTML 结构要求。我将严格按照用户的指示，将现有的 `src/views/Home.vue` 进行重构。

## 🏗️ 任务一：重构左侧导航栏 (Sidebar Upgrade)

### 1. 样式调整 (`src/views/Home.vue` <style>)
- **宽度**: 调整为 `100px` (用户指定 100-110px)。
- **背景**: 设置为 `#f7f8fa` (用户参考 CSS)。
- **边框**: 添加右边框 `border-right: 1px solid rgba(0,0,0,0.05)`。
- **选中态**:
  - 背景色: `#ffffff`。
  - 文字: 加粗，颜色 `#ff6b6b`。
  - 指示条: 左侧 `4px` 宽，颜色 `#ff6b6b`，圆角 `0 4px 4px 0`。

## 🎁 任务二：商品卡片“盒子化” (Product Card Redesign)

### 1. 结构调整 (`src/views/Home.vue` <template>)
- **容器**: 将 `goods-card` 重命名为 `product-box` 或保留类名但应用新样式。
- **布局**: 使用 `display: flex` 实现左图右文布局 (用户指定)。
  - **左侧**: 图片容器，固定尺寸 (如 `90px`)。
  - **右侧**: 信息容器，`flex: 1`。

### 2. 样式调整 (`src/views/Home.vue` <style>)
- **容器样式**:
  - 背景: `#ffffff`。
  - 圆角: `16px`。
  - 阴影: `0 4px 12px rgba(0,0,0,0.04)`。
  - 边距: `margin-bottom: 12px` (用户参考 CSS)。
  - 内边距: `12px`。
- **图片样式**:
  - 尺寸: `90px x 90px`。
  - 圆角: `12px`。
  - 背景: 极淡背景框。
- **排版**:
  - 标题: 加粗，字号加大。
  - 价格: 颜色 `#ff6b6b`，特大号字体 `18px`，前缀小号 `¥`。
  - 销量: 灰色小字底部。

## 🔧 任务三：修复“隐形”加号按钮 (Fix Add Button)

### 1. 结构调整 (`src/views/Home.vue` <template>)
- **替换按钮**: 移除原本的 `<button class="add-btn">`，替换为用户指定的 `<div>` 结构：
  ```html
  <div class="add-btn" @click.stop="addToCart(goods)">
    <el-icon><Plus /></el-icon>
  </div>
  ```

### 2. 样式调整 (`src/views/Home.vue` <style>)
- **位置**: `position: absolute; bottom: 12px; right: 12px;` (定位到卡片右下角)。
- **外观**:
  - 背景: `#ff6b6b`。
  - 颜色: `#fff`。
  - 尺寸: `28px` (用户参考 CSS) 或 `32px` (用户描述)。取 `32px` 以增加点击区域。
  - 形状: `border-radius: 50%`。
  - 阴影: `0 4px 8px rgba(255, 107, 107, 0.3)`。
- **交互**: `active` 状态缩放 `0.9`。

## 📝 执行计划
1.  **修改 `src/views/Home.vue`**:
    - 更新 `<template>` 部分，调整商品列表的 DOM 结构以支持左图右文布局。
    - 替换加购按钮的实现。
    - 大幅更新 `<style scoped>`，整合用户提供的 CSS 代码并覆盖现有样式。

---
*请批准执行，我将立即按照您的设计稿重绘首页！*