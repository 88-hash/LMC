**当前不一致点**
- C端支付后，我把订单状态更新为 2(待核销)，但 B 端“核销中心”与“订单管理”均按 0(待核销)/1(已完成)/-1(已取消) 的约定在查询。因此支付后数据不会进入 B 端待核销列表。

**统一的状态约定**
- 0：待核销（已支付，未核销）
- 1：已完成（已核销）
- -1：已取消

**改动范围（不改数据库结构，仅改代码逻辑）**
1) 后端
- OrderServiceImpl.pay：将支付成功后的状态更新为 0（而不是 2）。
- VerifyServiceImpl.confirmVerifyById：核销成功后将状态更新为 1（统一为“已完成”）。
- VerifyServiceImpl.listPending：保留按 0 查询待核销即可（当前实现已是 0）。

2) 前端 C 端（订单页）
- Order.vue 状态映射统一：
  - 0→“待核销”，展示核销码；
  - 1→“已完成”，按钮为“去评价”；
  - -1→“已取消”。
- 去支付按钮：保留“假动作”支付逻辑；支付完成后刷新列表即可看到状态 0 并显示核销码。

**自测场景**
- 下单→支付→B 端核销中心应出现该订单（状态 0）；
- 核销→状态变为 1，B 端订单管理筛选“已完成”能查到；
- C 端订单页支付后显示核销码，核销后显示“去评价”。

**说明**
- 不改任何表或字段，只统一状态数值的使用以打通 C→B 端链路。请批准执行，我将立即调整并自测。