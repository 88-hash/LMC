# 回退范围
- 仅回退上一轮前端改动：双排横向分类、收藏功能、两新增页面与路由、Profile店名与“我的收藏”入口。
- 不回退之前的性能/稳定性修复（如全局Loading、状态统一等），不触碰后端与数据库。

# 将回退的文件与内容
1) Home.vue
- 删除分类横向滚动块、收藏按钮与本地收藏逻辑、相关样式与路由依赖；恢复到改动前的商品列表结构。

2) router/index.js
- 移除 /favorites 与 /category-all 两条路由。

3) Profile.vue
- 去除顶部“乐逸零食店”展示和“我的收藏”入口；移除 Star 图标导入与样式新增。

4) 删除新增页面
- 删除前端页面：Favorites.vue、CategoryAll.vue。

# 验证
- 前端可编译运行，首页、购物车、订单、我的页面功能与样式恢复到回退前的状态（保留你认可的前一次稳定优化）。