# 改版目标（严格遵循你的指令）
- 不改后端接口与数据库，不加触感/等级/勋章/连续天数/优惠券等元素。
- 四个页面仍是：首页、购物车、订单、我的（评价入口保持）。
- 门店名统一展示为“乐逸零食店”。
- 新增：我的页面支持“编辑个人资料”和“我的收藏”（仅前端实现，不改后端）。

# 首页（Home.vue）
- 顶部品牌：显示“乐逸零食店”，保留圆角搜索框。
- 分类区域：双排方块、可横向滚动（scroll-snap），每块包含：高饱和背景色 + 3D 图标位（占位资源或静态SVG），中文分类名两行省略。
- 滑动末尾：药丸型“查看全部”按钮，点击进入完整分类树（前端页，不改后端）。
- Dopamine 卡片与热门列表：仅换皮肤与样式（渐变背景、圆角加阴影），保留“加购”逻辑。

# 购物车（Cart.vue）
- 列表保持原逻辑；仅卡片化样式（圆角、阴影、价格高亮）。
- 底部结算条保留现有行为与“去结算”；按钮文案使用“立即支付”或维持原文，整体中文。

# 订单（Order.vue）
- 列表卡片化，左侧竖色条按状态（待核销=黄、已完成=绿、已取消=灰）。
- 待核销显示核销码区域（已统一为状态0）；保留“刷新状态”。
- 评价入口保持现有路由与逻辑。

# 我的（Profile.vue）
- 顶部问候区：头像 + 文案“你好，昵称！”（保持中文）。
- 菜单卡：
  - 我的订单 → /order
  - 我的评价 → /my-comments 或现有评价入口
  - 编辑资料 → 弹窗表单（昵称、个性签名等）仅前端存储（localStorage，key=profile:{userId}），不改后端
  - 我的收藏 → 新前端页/抽屉，展示用户收藏的商品（见下）

# 我的收藏（无后端改动）
- 收藏来源：在首页商品卡加一个“♡”按钮（不影响加购），点击将 {userId, goodsId} 存入 localStorage（favorites:{userId}）。
- 我的收藏页：网格展示已收藏商品（从现有 /goods/list 过滤），支持一键加购与取消收藏。
- 所有文案中文；不改任何接口或数据库。

# 资源与图标
- 分类 3D 图标位：优先本地静态SVG或占位图片；确保不影响数据。
- 渐变与色板：统一多巴胺色系，但不引入新依赖。

# 导航与路由
- TabBar 保持不变；新增“查看全部分类”跳转仅前端页面。
- 404 与 Loading 已完成，无需重复变更。

确认以上理解后，我将按此方案逐页改皮肤与结构（仅前端），不触碰后端与数据库。