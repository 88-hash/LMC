# 🚑 紧急修复与流程回滚计划 (Fix & Revert Plan)

**用户反馈确认**:
1.  **购物车**: “去结算”按钮隐形（但可点击），复选框无法点击，只能用删除操作。
2.  **支付**: 需要回滚为“假动作”模拟支付，暂时不调用后端 API。
3.  **核销**: 仅展示数字核销码，暂时移除二维码。

## 🛠️ 修复步骤

### 1. 修复购物车显示与交互 (`src/views/Cart.vue`)
*   **按钮隐形修复**: 这是一个 CSS 变量缺失问题。我将把 `var(--color-primary)` 替换为硬编码的 `#ff6b6b` (西瓜红)，确保按钮背景色可见，并设置文字颜色为白色。
*   **复选框修复**: 检查 `el-checkbox` 的事件冒泡，尝试添加 `@click.stop`，并确保 `item.selected` 是响应式的。同时检查是否有遮挡层。

### 2. 回滚支付逻辑 (`src/views/Cashier.vue`)
*   **动作**: 注释掉 `/order/pay` 的 API 调用。
*   **恢复**: 使用 `setTimeout` 模拟 1.5秒 Loading 后直接跳转成功页，保持演示流程的流畅性。

### 3. 简化核销展示 (`src/views/Order.vue`)
*   **动作**: 移除 `qrcode.vue` 组件及相关代码。
*   **恢复**: 仅保留“取货核销码”的大字展示 (12位数字)，还原到之前的简洁版本。

### 4. 流程自测
*   **模拟**: 加购 -> 购物车勾选(测试点击) -> 结算(测试按钮可见性) -> 支付(测试假动作) -> 核销(测试纯数字码)。

---
*请批准执行，我将快速修复 UI 问题并回滚部分逻辑，确保演示流程正常。*