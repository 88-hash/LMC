**目标**
- 保持数据库表结构不变，确保“所有商品都能加购”，且订单页正常打开。

**要点**
- 前端：加购只取 `goods.id`，若缺失则立即提示并阻止请求；打印商品名与 id，便于排查。
- 后端：`/cart/add` 入参校验 `goodsId` 非空/大于 0；失败返回清晰业务错误。
- 不改任何数据库表或字段。

**实施步骤**
1) 前端 [Home.vue](file:///c:/Users/lmc/Desktop/LeYi_Snack_System/frontend/src/views/Home.vue)
- 在 `addToCart(goods)` 中：
  - 取 `const gid = Number(goods.id)`；若 `!gid` 或 `NaN`，`ElMessage.error('商品ID缺失：'+goods.name)` 并 `return`。
  - 请求体使用 `{ goodsId: gid, quantity: 1 }`；打印 payload 与响应。

2) 后端 [CartController.java](file:///c:/Users/lmc/Desktop/LeYi_Snack_System/backend/src/main/java/com/leyi/snack/controller/CartController.java)
- 在 `/cart/add` 方法：
  - `if (dto.getGoodsId()==null || dto.getGoodsId()<=0) return Result.error("商品ID缺失");`
  - 保留 `try-catch`，将异常消息返回给前端。

3) 自测
- 首页随机点击 10 个商品（含非“老坛酸菜面”），观察前后端日志是否都有 `goodsId` 且成功返回。
- 打开 [Order.vue](file:///c:/Users/lmc/Desktop/LeYi_Snack_System/frontend/src/views/Order.vue) 页面验证正常加载。