我将执行以下操作来修复 Maven 依赖报错：

1.  **运行修复命令**: 在 `backend` 目录下执行 `mvn clean compile -U`。
    *   这将强制 Maven 重新下载依赖并重新编译项目，消除 IDE 的缓存错误。

2.  **预期结果**:
    *   如果终端显示 `BUILD SUCCESS`，证明 `pom.xml` 配置无误，报错可以忽略。
    *   如果失败，我将根据终端输出的真实错误信息进行调整。

请确认执行？