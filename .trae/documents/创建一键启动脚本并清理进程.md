我已定位到问题：`OrderServiceImpl` 中的 `pay` 方法在接口 `OrderService` 中未定义，导致报错。`pom.xml` 的配置是兼容的（Spring Boot 3.2.0 + MyBatis 3.0.3），红色波浪线通常是依赖未下载完成导致的，运行启动命令时会自动修复。

我将执行以下修复和脚本创建计划：

### 1. 修复代码报错
*   **修改文件**: `backend/src/main/java/com/leyi/snack/service/OrderService.java`
*   **操作**: 在接口中添加 `void pay(String orderNo, String payMethod, Long userId);` 方法定义，解决 Override 报错。

### 2. 创建一键启动脚本
*   **创建文件**: `c:\Users\lmc\Desktop\LeYi_Snack_System\start_project.bat`
*   **脚本逻辑**:
    1.  **环境清理**: 强制关闭旧的 `java.exe` (后端) 和 `node.exe` (前端) 进程，释放 8080 和 5173 端口。
    2.  **启动后端**: 打开新窗口运行 `mvn spring-boot:run`（此步骤会自动下载 `pom.xml` 中缺失的依赖，修复红色波浪线）。
    3.  **启动前端**: 打开新窗口运行 `npm run dev`。
    4.  **自动访问**: 等待 15 秒（预留后端启动时间）后自动打开浏览器访问 `http://localhost:5173`。

请确认执行此计划？