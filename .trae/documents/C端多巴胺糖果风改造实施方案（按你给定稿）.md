## 总体目标
- 将首页、购物车、订单、个人中心视觉改造成你提供的风格稿（多巴胺糖果风），保持中文与现有业务逻辑、API 调用不变。
- 只改前端样式与结构：style.css、Layout.vue、Home.vue、Cart.vue、Order.vue、Profile.vue。

## 全局（style.css）
- 新增设计系统变量：
  - 颜色：bg-cloud(#F7F9FC)、card-white(#FFF)、primary-pop(#FF6B6B)、accent-blue(#48DBFB)、text-dark(#2C3E50)、text-gray(#95A5A6)
  - 圆角：radius-box(20px)、radius-btn(99px)
  - 阴影：shadow-soft(0 8px 24px rgba(149,157,165,0.1))
- 通用类：
  - app-shell（100vh、overflow:hidden）、content-area（flex:1、overflow-y:auto）
  - card、btn-primary/btn-outline、price、tag（yellow/green/gray）、glass（玻璃态）
- 隐藏滚动条外观但保留滚动；保留按钮点击动画

## 布局容器（Layout.vue）
- 应用 app-shell + content-area：顶部 NavBar 固定，内容区滚动，底部 TabBar 固定（与你稿件一致）
- 统一背景为 bg-cloud，安全区内留白；保持 cartStore.refresh 等逻辑不变

## 首页（Home.vue）
- 顶部区：
  - 渐变头图（与稿件一致的暖色渐变）；左上品牌与“雷依小吃店/李先生的小店”副标题；胶囊搜索框样式
- 左侧 Sidebar（25% 宽、独立滚动）：
  - 选中项“白色悬浮胶囊 + 主色文字”，未选“透明灰卡”样式
  - 在“全部/分类列表”上应用胶囊样式（不改点击逻辑）
- 右侧 ProductList（75% 宽、独立滚动）：
  - 顶部“人气推荐”卡片（淡粉色背景 + 糖圈图标位 + 小字副标题）
  - 商品卡：
    - 白色 Bento 卡片（圆角/阴影）；左侧灰色占位图或商品图；右侧名称、销量标签；右下主色加购“+”圆按钮
    - 价格主色红、加粗，与右端“+”对齐
- 骨架屏：加载时以白卡骨架代替

## 购物车（Cart.vue）
- 列表卡结构：
  - 每条为 白卡（圆角/阴影），左侧商品图；右侧标题 + 红色价格 + 删除图标
  - 左侧选择圆形复选标：红色选中态
- 数量控制器：
  - 胶囊容器内 [-] [数量] [+]，按钮圆形、加粗；绑定现有 incr/decr/onChange 不变
- 底部结算栏（玻璃态）：
  - 左侧“全选/已选/合计”信息，右侧“去结算”渐变主按钮；位置在 TabBar 之上
- “猜你喜欢”区：样式占位（使用现有 goods 列表取前3个，不新增接口）

## 订单（Order.vue）
- 列表卡：
  - 票据卡样式（白卡、圆角、阴影）；顶部“订单号 + 状态标签（绿/橙）”居左；右侧红色总价
  - 商品明细：灰底小卡列表，超出 3 个时显示 +N 折叠标
- 刷新条：顶部蓝色淡条（“点击刷新订单状态”）
- 操作按钮：右下角“去评价”绿胶囊；待核销显示核销码玻璃块（居中加粗）
- 保持状态映射：0 待核销、1 已完成、-1 已取消

## 个人中心（Profile.vue）
- 头部大卡：顶部渐变背景，居中圆形头像（白描边），昵称白色加粗，手机号淡白；右下“编辑资料”白描边胶囊按钮
- 菜单列表：
  - 每项为白卡（图标方块 + 文字 + 右箭头），加大间距与阴影：我的订单 / 购物车 / 我的评价
- 底部：淡红色“退出登录”胶囊按钮；版本号浅灰
- 编辑资料弹窗逻辑保持不变

## 无障碍与触控
- 交互最小触控 44×44；文本对比度；按钮禁用/加载统一样式

## 风险与回滚
- 完全新增类、不删除旧类；任何异常时可快速切换旧样式
- 不改接口与路由；严格保留现有事件与 v-model

## 交付与验收
- 一次性提交 5 个文件的改造（style.css、Layout.vue、Home.vue、Cart.vue、Order.vue、Profile.vue）
- 验收：移动端 390×844 下效果与稿件一致；交互无异常；滚动正常但不可见滚动条

## 执行顺序
1) style.css 设计系统落地
2) Layout.vue App Shell 落地
3) 首页样式改造
4) 购物车样式改造
5) 订单样式改造
6) 个人中心样式改造

确认后我开始实施，并逐页提交代码供你审核。
