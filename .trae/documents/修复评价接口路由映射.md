# 🚑 紧急修复计划：评价接口 404 问题

## 问题诊断
用户在前端点击发布评价时遭遇 404 错误。
**根本原因**：前端 Vite 代理配置了路径重写 (`rewrite: path => path.replace(/^\/api/, '')`)，导致发送到后端的请求路径被去除了 `/api` 前缀（即 `/comment/add`）。然而，后端 `CommentController` 错误地定义了 `@RequestMapping("/api/comment")`，导致路径不匹配。

## 修复步骤

### 1. 修正后端路由映射
- **文件**: `backend/src/main/java/com/leyi/snack/controller/CommentController.java`
- **操作**: 将 `@RequestMapping("/api/comment")` 修改为 `@RequestMapping("/comment")`，以匹配经过代理重写后的请求路径。

### 2. 增强接口安全性 (WebMvcConfig)
- **文件**: `backend/src/main/java/com/leyi/snack/config/WebMvcConfig.java`
- **操作**: 在 `LoginInterceptor` 的拦截规则中添加 `/comment/**`，确保未登录用户无法调用评价接口。
    - *理由*：评价功能涉及用户身份（需从 Token 解析），且写入数据库，必须受权保护。

### 3. 重启后端验证
- **操作**:
    1. 停止后端服务。
    2. 执行 `mvn spring-boot:run` 重启。
    3. 前端再次尝试提交评价，验证是否成功。
